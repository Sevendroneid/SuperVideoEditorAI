# Live preview access

The real-world preview workflow publishes the temporary TryCloudflare URL in the `supervideo-live-preview` workflow artifact as `LIVE_PREVIEW_URL.txt` and `LIVE_PREVIEW.md`.

The URL is valid only while the workflow run remains alive.
