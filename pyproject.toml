[project]
name = "super-video-editor-ai"
version = "0.2.1"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.116.1",
    "pydantic-settings==2.10.1",
    "python-multipart==0.0.20",
    "httpx==0.28.1",
]

[tool.vercel]
entrypoint = "api.index:app"
